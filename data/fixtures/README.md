# Deterministic Contract Fixtures

These fixtures exercise public schemas without using real customer information.

They are intentionally small and provider-independent. Later phases may add new fixtures, but existing examples should remain stable unless a versioned contract change requires replacement.
