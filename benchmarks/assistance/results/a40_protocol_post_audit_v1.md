# Phase 4 A4.0 protocol post-audit

**Verdict: PASSED**

A4.0 freezes the evidence-grounded assistance methodology before any generator, prompt, verifier, or evaluation model is bound and before any assistance result is opened.

The tested pre-audit head was `61b098530fb72554949f4df56ddea88698aa25f9` on pull request #14. GitHub Actions run `32308703333`, job `96246985132`, completed successfully.

## Repository quality gate

- Ruff check: passed.
- Ruff format check: passed.
- mypy: passed with no issues.
- pytest: **118 passed, 0 failed**.
- Phase 1 offline data contracts: passed.
- Phase 3 retrieval preflight: passed with 147 eligible documents, seven current conflict fixtures, five current untrusted-content fixtures, and no retrieval scoring generated by the preflight.
- Public/private publication audit: passed.

The previously recorded FastAPI/Starlette TestClient deprecation warning and upstream GitHub Actions Node compatibility warnings remain non-blocking maintenance items.

## Protocol reconstruction

The protocol still points to the exact frozen `helixbank-policy-v1.0.0` substrate: 154 documents, 308 queries, 616 judgments, and 77 intents. Query semantics reconstruct to 77 answerable, 77 ambiguous, 77 outdated-evidence, 70 missing-evidence, and seven conflicting-evidence cases.

The deterministic partition is by intent, not query. It reconstructs to 60 development intents / 240 queries and 17 confirmatory intents / 68 queries, with five conflicting-evidence queries in development and two in confirmation. All four query variants of an intent stay together.

The bounded candidate ladder remains `G0`, `G1`, and `G2`. The primary endpoint remains strict grounded success rate. H1 remains `G1 - G0` on that endpoint. H2 remains `G2 - G1` on unsupported-sentence rate. Inference remains a paired nonparametric cluster bootstrap by intent with 5,000 replicates and seed `20260819`.

The adversarial suite remains frozen at 77 direct-injection cases, 77 citation-spoof cases, 20 indirect-injection cases, and seven archived-evidence distractor cases.

## Scope audit

No generation candidate has been implemented or scored. No generator identity, prompt bytes, runtime verifier, independent evaluation verifier, latency ceiling, or cost ceiling has been bound. No Phase 2 or Phase 3 scientific code was changed. No R3.2 evidence, `retrieval-selected-v1`, or HelixBank record was modified.

Gold citations, expected decisions, relevance grades, case labels, conflict labels, and untrusted-content labels remain evaluator-only fields and are prohibited from candidate input. Generation isolation remains the primary comparison surface; retrieval-plus-assistance evaluation stays deferred until a generation candidate is selected.

Candidate self-reported unsupported claims remain telemetry only. The registered grounding verdict must come from the separately frozen evaluation process. G2's runtime verifier must be from a different model family than the independent evaluation verifier, and verifier failure cannot trigger answer repair or regeneration.

## Defects caught before closure

The repository gate caught two formatting defects in the newly added protocol guard test. The first was three over-length lines; the second was a mismatch with Ruff's canonical layout. Both were repaired without changing any methodological assertion.

## Closure condition

A4.0 is ready to close after the audit artifacts themselves pass the repository-wide CI gate. A4.1 is the only permitted next binding checkpoint. It must freeze exact generator and verifier identities, immutable revisions where available, prompt bytes, remaining runtime/evaluator constants, deterministic repeatability and latency subsets, candidate-specific P95 latency ceilings, and candidate-specific maximum estimated cost per request before any assistance scoring occurs.
